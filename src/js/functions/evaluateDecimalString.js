import { convertDecimalToBase } from './convertDecimalToBase'
import { symbols } from './symbols'
import { funSymbols } from './funSymbols'

const evaluateDecimalString = (decimalString, outputBase) => {
 return (`${decimalString} ${outputBase}`)
}

export { evaluateDecimalString }